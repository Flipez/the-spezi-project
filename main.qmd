---
title: "The Spezi Project"
format: html
---
```{python}
#| code-fold: true
#| code-summary: "Show the code"

import plotly.express as px
import pandas as pd
df = pd.read_csv('data.csv')
```

```{python}
fig = px.bar(df.sort_values("Gesamtbewertung", ascending=False), x="Name", y="Gesamtbewertung", color="Typ", title="Long-Form Input")
fig.show()
```

```{python}
fig = px.density_heatmap(df, x="Suesse", y="Fruchtfaktor", facet_row="Zero", facet_col="Typ")
fig.show()
```

# Spezi Suesse Spread
ers;gndlighnisdghfdth
```{python}
#| code-fold: true
#| code-summary: "Show the code"


var = "Spezi"
gb = df.query("Typ == @var").groupby(by=["Suesse", "Fruchtfaktor", "Typ"])["Suesse"].agg("count").to_frame('Count').reset_index()

fig = px.scatter(gb,
                 x="Suesse",
                 y="Fruchtfaktor",
                 title="Spezis",
                 size="Count",
                 color="Count",
                 color_continuous_scale=px.colors.sequential.Viridis,
                 template="plotly_white")

fig.update_layout(title=dict(font=dict(size=50), automargin=True, yref='paper'))
fig.show(config=dict({'staticPlot':True}))
```

# Spezi & Cola Database
```{python}
#| code-fold: true
#| code-summary: "Show the code"

from itables import init_notebook_mode

init_notebook_mode(all_interactive=True)
df
```